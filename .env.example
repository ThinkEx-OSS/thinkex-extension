# Better Auth backend URL (required when using Better Auth)
# Use http://localhost:3000 for local dev, your production URL when deployed
WXT_BETTER_AUTH_BASE_URL=http://localhost:3000

# Add other env vars below. Prefix with WXT_ or VITE_ to expose at runtime.
